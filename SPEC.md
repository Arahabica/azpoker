# 暗算ポーカー 仕様

**暗算ポーカー｜勝率と確率のドリル**

この文書は、ゲームルール、出題条件、保存データなど、長期的に維持する製品仕様だけを記録します。個別画面の装飾、実装手順、作業進捗、一時的なタスクは記録しません。製品仕様そのものが変わる場合にだけ更新します。

デザイン原則は [`DESIGN.md`](DESIGN.md)、起動方法とコード構成は [`README.md`](README.md) を正とします。

## セッション

| 項目     | 内容                                                      |
| -------- | --------------------------------------------------------- |
| 1セット  | 常に10問。履歴に誤答があれば、その1問と新しい9問を出題    |
| モード   | A 4問、B 3問、C 1問、D 2問                                |
| ステージ | プリフロップ3問、フロップ4問、ターン3問                   |
| 難易度   | 中8問、難2問                                              |
| 対象者   | 初心者7問、中級者3問。上級者向けは通常出題しない          |
| 0%       | 完成不可能な問題は出題しない                              |
| 重複     | Aは別カテゴリ、Dは別系統から選び、直近500問を再出題しない |

直近10セットに誤答または時間切れがあれば、その中から1問をランダムに選び、その履歴問題を含めて合計10問になるよう残り9問を選ぶ。

### 制限時間

各問題は、回答可能になってから回答または時間切れまでを計測する。各問の時間は保存せず、通常問題の開始から必要な復習をすべて終えるまでの合計回答時間と合計制限時間だけを結果と履歴へ保存する。平均時間は表示しない。

| 問題                      | 制限時間 |
| ------------------------- | -------: |
| 1ハンド・中・プリフロップ |      5秒 |
| 1ハンド・中・フロップ     |      8秒 |
| 1ハンド・中・ターン       |     12秒 |
| 難問                      |     16秒 |
| 2ハンドを表示するモードB  |     16秒 |

- 0秒になった問題は時間切れの不正解として扱う。
- 回答、時間切れ、次の問題への移動、途中退出時にタイマーを停止し、二重回答を受け付けない。

## 結果と履歴

- 結果には正答数、全問題数、合計回答時間、合計制限時間を表示する。時間切れがある場合だけ回数も表示する。
- 全問正解の結果または復習終了後の結果へ到達した時点で、日時、通常出題の正答数と全問題数、復習を含む合計回答時間と合計制限時間、通常出題の時間切れ数、各問題のスナップショット、選択回答、正誤をLocalStorageへ保存する。
- 同じセッションを二重保存せず、新しい順で最大50件を保持する。
- トップには新しい2件を表示し、履歴一覧は `/history`、履歴詳細は `/history/{履歴ID}` とする。
- 履歴詳細では1セットを縦に閲覧でき、各問題の難易度、選択回答、正解、正誤、実際の値、解説を確認できる。
- 履歴詳細への導線は、トップから開いた場合はトップへ戻り、履歴一覧から開いた場合は履歴一覧へ戻る。

## 復習

- 通常問題をすべて終えた時点で誤答または時間切れがあれば、中間の結果画面や開始ボタンを挟まず自動的に復習へ進む。全問正解なら復習へ進まず結果を表示する。
- 直前のセットで間違えた問題を1問ずつ復習キューへ入れ、問題と同時に難易度を表示する。
- 復習中に正解できなかった問題はキュー末尾へ戻し、すべての問題へ正解するまで完了しない。
- 復習終了後は通常の結果画面を表示し、通常出題の正答数と全問題数、復習を含む合計回答時間と合計制限時間を確認できる。
- 復習中の回答は個別の履歴として保存せず、通常出題の履歴へ復習を含む合計時間だけを反映する。

## 効果音

- 初期状態は音ありとし、1セットごとに音あり・音なしを選べる。
- 通常問題のスタート、正解、不正解・時間切れ、時間警告、通常結果、全問正解を別の短い効果音で伝える。自動で始まる復習ではスタート音を鳴らさない。
- 音なしではすべての効果音を停止する。
- 音声の読み込みや再生に失敗しても、問題の開始、回答、結果表示を止めない。

## 問題バンク

問題カテゴリ、学習目的、件数、対象者、ステージ、出題系統は [`questions/patterns.md`](questions/patterns.md) を正本とする。正本からPython・TypeScriptの定義を生成し、生成済み問題の全カテゴリと件数が正本に一致することを検査する。確率計算とカード生成のアルゴリズムはテスト可能なコードとして管理する。

2つの手札を比べる問題は、その2ハンドだけの直接対決として解説する。ランダムな5人を相手にする6人卓のスターティングハンド勝率表とは条件が異なるため、直接対決の細かい値の確認先として同表を案内しない。

トップページのスターティングハンド勝率表は [`questions/preflop-equities.md`](questions/preflop-equities.md) を正本とし、問題バンクのモンテカルロ計算値とは分けて管理する。正本には6人卓・9人卓とも、ランダムな相手とリバーまで進んだ100万回のシミュレーションによる掲載値を保持し、トップページでは6人卓の値だけを使う。表内はハンド名と色だけを表示し、16%・22%・25%・30%を境に5段階へ分類する。セクション全体は「テキサスホールデム」と同じカード型のデザインとし、13列の表だけをカードの枠から左右へ広げてスマートフォンの横幅近くまで表示し、横スクロールさせない。表の透明色が外側の背景に影響されないよう、表の直下にはセクションと同じ濃緑の不透明な下地を敷く。卓人数の補足では、7人卓でも最初の1人がフォールドし、自分のあとに判断する相手が5人なら、おおむね同じ基準であることを示す。表は「テキサスホールデム」の説明後に「付録」として配置し、画面上に出典と計算条件を示す。

問題は生成時に確率と勝敗を計算し、本番ではA、B+C、Dから100問ずつ取得する。初期JavaScriptへ問題本体を含めず、取得した300問は複数セッションで再利用する。

| モード | 問題数 | 内容                                                     |
| ------ | -----: | -------------------------------------------------------- |
| A      | 10,000 | 役、ランク、ボード変化の確率                             |
| B      |  4,800 | 2つの手札の比較と、逆転・引き分けなどの確率              |
| C      |  1,200 | プリフロップの6人・9人勝率とポストフロップの2人・6人勝率 |
| D      |  5,000 | 相手のランク、完成役、危険ボード、ドローの可能性         |
| 合計   | 21,000 | 100問単位のJSON 210ファイル                              |

### モードA

| カテゴリ                   | 問題数 |
| -------------------------- | -----: |
| フラッシュ                 |  1,400 |
| ストレート                 |  1,600 |
| フラッシュまたはストレート |    800 |
| 指定ランクが出る           |    550 |
| 指定ランクがスリーになる   |    500 |
| ツーペア                   |    400 |
| フルハウス                 |    400 |
| フォーカード               |    250 |
| ストレートフラッシュ       |    100 |

通常のフラッシュドロー、OESD、ガットショット、重複アウツを扱う。完成不可能な0%とバックドアストレートは出さず、すでに対象が完成している状況も出さない。

加えて、通常のフラッシュドロー・OESD・ガットショットを各500問、バックドアフラッシュを200問、ボードのペア・ツーペア、オーバーカード、4フラッシュ、カウンターフィット、役の維持を扱う。バックドアフラッシュは、手札とフロップに同じマークがちょうど3枚あり、ストレート待ちを併設しない初心者向け問題だけにする。ナッツフラッシュ問題は作らない。

指定ランクの出現問題は、そのランクを手札に1枚持ち、来ればペア以上になる場合だけ出題する。コンボドローでは、フラッシュとストレートの両方を完成させる1枚を二重計上しない。

### モードB

「勝率が高いのは？」3,000問に加え、右の手札の勝率を答える問題を1,800問用意する。数値問題は、現在負けている手札と現在勝っている手札を各900問とする。両ハンドとも現在のボードで続行する理由を必須とする。比較問題は、ペア対オーバーカード、プレイ可能な初期手札同士、トップペア対フラッシュドロー、ドロー対ツーペア以上、キッカー勝負、コンボハンドへ類型別に寄せる。プリフロップでは、ペア以外で一方の上位・下位カードがともに高い形と、同じ上位カードでキッカーだけが高い形を明白な対決として除外する。同じマークと連続性は明白判定に使わず、勝率計算には反映する。勝率を尋ねる対象は右側へ配置し、5〜95%の範囲に限定する。フロップの数値解説には残り2枚の全組合せ数を表示せず、現在の役、双方の強み、キッカー、再逆転、引き分けを比較して「4回に3回」などの暗算目安を示す。1セットには手札選択型2問（うちプリフロップ1問以上）と数値型1問を出す。51〜55%の接戦も難問として残す。

### モードC

169種類の初期ハンドを6人・9人で1問ずつ用意し、さらにフロップ・ターンの2人卓431問、6人卓431問を用意する。プリフロップの6人卓は初心者向け、9人卓は中級者向けとし、1セットにどちらか1問を必ず出す。ポストフロップの6人卓は上級者向けとして通常セッションから外す。スート違いで水増しせず、別人数の勝率を誤答へ流用しない。トップページには表示専用の正本から6人卓の169ハンドを使った勝率表を生成し、9人卓の値は問題用・比較用データとして保持する。

### モードD

指定ランクに加え、ポケットペア、オーバーペア、セット、ボードで一番高いランクを持つ可能性、ツーペア、ストレート、フラッシュ、OESD、ガットショット、フラッシュドロー、コンボドロー、上のフラッシュ、同じペアで上のキッカー、全員のミス、指定ランクを持つ人数を扱う。相手の役・ドローはホールカードを最低1枚使い、ハンドレンジやアクションは考慮しない。

相手の人数は問題文に明記する。6人卓とテーブル全体の人数を扱う問題は上級者向けとして通常セッションから外す。

## 選択肢

概算値は `0 / 1 / 2 / 3 / 5 / 7.5 / 10 / 12.5 / 15 / 17.5 / 20 / 25 … 100%` から、実際の値に最も近いものを使う。回答後は小数1桁を表示する。

誤答には `distractorModel` を持たせ、残り枚数の混同、重複アウツの二重計上、バックドアの誤認、見えている同ランクの引き忘れ、人数の考慮漏れ、キッカー・完成役・ドローの過大評価など、典型的な勘違いから作る。別人数や別ステージの値を流用した捨て選択肢は使わない。

正解が20%以上なら選択肢同士を最低10ポイント、5〜17.5%なら最低5ポイント、1〜3%なら最低2ポイント離す。実際が1.9%なら正解は約2%とし、誤答には5%以上を使う。0%の誤答は3%以上とする。

## 計算と生成

- AとBのフロップ・ターンは残りカードを全列挙する。
- Bのプリフロップは12,000試行、Cは30,000試行の固定seedモンテカルロを使う。Cはハンド単位で並列計算する。
- Dの2人卓は相手の2枚を全列挙し、6人卓は重複なしで5人へ配る固定seedモンテカルロを使う。
- `pnpm patterns:build` で正本からパターン定義を生成する。`pnpm questions:build` でパターン定義と問題を生成し、`pnpm check:bank` で正本、生成済み21,000問、3パックのmanifestを検査する。
- `pnpm preflop-equities:build` で表示用の勝率表を生成し、`pnpm preflop-equities:check` で169ハンドの正本との一致を検査する。
- 同型判定ではスートを初出順に正規化し、スート名だけを置換した問題を重複させない。

## 表示文言

- 「3カード」は使わず「スリー」とする。
- 「スート」や「ランク」はそのまま表示せず、カードを見れば分かる初心者向けの言葉へ置き換える。
- 問題文は対象を判断できる短さにし、条件、対象の数字、残り枚数を直接書く。
- 専門用語を使う場合は日本語の意味を先に示す。
- 回答後の解説は、別表示される実測値を繰り返さず、「何を数えるか → 暗算方法 → 覚え方」の順で書く。
- 1枚で完成するドローは、フロップなら「アウツ×4」、ターンなら「アウツ×2」を概算の基準にする。残り2枚の両方が必要な場合や、相手の組合せ・多人数勝率にはこの基準を使わない。
- フラッシュとストレートを同時に待つ場合は、両方を完成させるカードを一度だけ数える。6人卓は1人分を単純に5倍せず、相手5人へ重複なく配られることを説明する。

# 公開準備

暗算ポーカーを `https://azpoker.me/` で公開するまでの作業をまとめる。ゲーム仕様は [`PLAN.md`](PLAN.md)、画面と演出は [`DESIGN.md`](DESIGN.md)、素材の原典とライセンスは [`THIRD_PARTY_NOTICES.md`](THIRD_PARTY_NOTICES.md) を正とする。

## 実装順

1. [x] ページ遷移と履歴保存の基盤
2. [x] トップページのLP化と履歴表示
3. [x] 利用規約、素材、開発者情報
4. [ ] OGP画像とメタ情報
5. [ ] ドメイン取得・接続
6. [ ] 公開前確認

ドメイン取得は実装と並行して進められる。OGP画像は完成したトップページを元にするため、LP確定後に作る。

## ドメイン

- Cloudflare Registrarで `azpoker.me` を取得する。空き状況と価格は購入画面で最終確認する。
- 登録メールの認証、自動更新、DNSSECを確認する。
- 既存のCloudflare Pagesプロジェクトへカスタムドメインとして接続する。
- 正規URLを `https://azpoker.me/` とする。
- `www.azpoker.me` は正規URLへリダイレクトする。
- canonical URLとOGPの絶対URLにも同じドメインを使う。

ドメインの購入とCloudflareアカウント上の最終操作は開発者が行う。

## ページ構成

次のURLを用意し、URLを直接開いた場合やブラウザの戻る・進むでも正しく表示できるようにする。

| URL        | 内容                             |
| ---------- | -------------------------------- |
| `/`        | トップ、LP、クイズ               |
| `/history` | 保存したクイズ結果の一覧         |
| `/terms`   | 利用規約                         |
| `/credits` | 素材、ライセンス、開発者について |

大きなルーターライブラリは追加せず、現在のSvelte構成に合う軽量なページ遷移にする。公開情報を含むページはCloudflare Pagesで直接表示できる静的なHTMLを生成する。

## トップページのLP化

ファーストビューでは、ロゴカード、タイトル、短い目的、開始操作だけを見せる。

- タイトル: `暗算ポーカー`
- サブタイトル: `確率を瞬時に判断して、もっと強くなろう！`
- 開始操作: `クイズをはじめる`

ファーストビューの次は、「この問題すぐ答えられますか？」と実際のカード、問題文、2つの選択肢だけで`100dvh`を使う。問題を考える前に答えやサービス説明が見えない構成にする。

その直後に、表示カードと確率エンジンの結果が一致する答えを次の形で見せる。

> 答えは約35%（35.0%）です。

続いて、「カードを見た瞬間に確率が浮かぶようにして強くなりましょう！」を通常の段落として置く。見出しにはせず、続く本文と同じ書体・配置にする。テキサスホールデムの10問の確率問題を制限時間付きで解くクイズアプリであること、スキマ時間にポーカーの基礎体力を上げられることを短い文章で伝える。最後に「そもそもテキサスホールデムって何？」からルール説明へつなぐ。2択、制限時間、回答後の確認などを個別の機能一覧にはしない。

### 最近の履歴

保存済みの結果が3件以上ある場合だけ、ヒーロー内で最初の`クイズをはじめる`の直前に新しい2件を表示する。各結果は相対時間、正答数、合計時間を1行にまとめる。見出しは小さくし、「もっと見る」はリストの下へ白の控えめな文字で置く。ヒーローは`100dvh`を使い、3件未満では履歴領域を描画しない。

### テキサスホールデムとは

初心者向けに、手札2枚とテーブルに開く5枚の共通カードから、最も強い5枚を選んで役を作るゲームであることを短く説明する。共通カード5枚と手札2枚は文章だけでなく、アプリと同じカード面を使って図示する。

詳しいルールは[AJPCの解説](https://www.ajpc.jp/about-poker/)へ案内し、外部サイトであることをリンク文言で示す。

### 下部の開始操作

説明を読んだ後にも、そのまま開始できるよう、フッターの直前に`クイズをはじめる`をもう一度置く。

### フッター

- `ご感想・ご要望お待ちしております！`
- `Xで開発者に連絡する`
- `利用規約`、`素材`、`開発者`

ヒーローとLP本文には、それぞれ別のKosugi Maruサブセットを使う。英数字だけM PLUS Rounded 1cを使い、`%`はKosugi Maruで表示する。初期HTMLではヒーロー用Kosugiだけを先読みし、LP本文用とゲーム画面用を分離する。履歴一覧、利用規約、素材・開発者情報はシステムフォントを使う。

### LPの参考

- [POKER Q'z](https://pokerqz.com/ja): 機能より先に学習成果を示し、実際のトレーニング画面へつなぐ構成
- [確率の考え方を大事にしよう](https://note.com/gametrainer/n/n22d9d2838ba2): 直感と実際の確率がずれることを訓練の動機として扱う考え方
- [ポーカー 確率トレーナー](https://play.google.com/store/apps/details?id=com.AND48.ProbabilityTrainer&hl=ja): 抽象的な機能説明より具体的な問題例を先に見せる方法
- [ポーカー フロップトレーナー](https://play.google.com/store/apps/details?id=com.AND48.FlopTrainer&hl=ja): GTOやレンジ分析を扱う経験者向けサービスとの差異
- [PokerListings オッズ計算機](https://www.pokerlistings.jp/poker-tools/poker-odds-calculator): 指定場面を調べる計算機と、先に自分で答えるトレーナーの役割の違い
- [Poker Trainer Quizzes](https://pokertrainer.se/quizzes/): 実物のクイズと即時フィードバックで訓練内容を伝える方法
- [AJPC テキサスホールデムのルール](https://www.ajpc.jp/about-poker/): 初心者向けルール説明の根拠と詳細リンク

## 履歴

### 保存

クイズを最後まで完了して結果画面へ移ったときに、次の情報をLocalStorageへ保存する。

- 一意なID
- 完了日時
- 正答数
- 全問題数
- 回答に使った合計時間
- 合計制限時間
- 時間切れ数
- 保存形式のバージョン

保存キーにはアプリ名とバージョンを含め、最大50件を新しい順で保持する。51件目を保存したら最も古い結果を削除する。問題内容や手札など、表示に不要な情報は保存しない。

結果画面への再描画やブラウザ操作による二重保存を防ぐ。LocalStorageが無効、容量不足、または保存値が壊れている場合も、クイズ自体は継続できるようにする。

### トップページ

履歴が3件以上ある場合だけ、新しい2件について「5分前　8/10問　44.5秒」のように相対時間、正答数、合計時間を1行で表示する。3件未満では履歴セクション自体を表示しない。

### 履歴一覧

`/history` に最大50件を新しい順で表示する。履歴がない場合は空状態とトップへ戻る導線を表示する。今回の公開範囲には、アカウント同期、サーバー保存、履歴の個別削除は含めない。

## 利用規約

暗算ポーカーの提供内容と実装に合わせた文章を用意する。

- 規約の適用
- サービス内容
- 利用条件と禁止事項
- 知的財産権
- 免責事項
- 情報の保存と取り扱い
- 規約の変更
- 準拠法と管轄
- 問い合わせ先

確率や勝率は学習を目的とした表示であり、実戦結果、利益、特定の判断を保証するものではないことを明記する。履歴は利用者のブラウザのLocalStorageだけに保存し、アカウントやサーバーへ同期しないことも説明する。

アクセス解析などを導入していない段階では、存在しない情報収集について記載しない。将来導入する場合は、公開前に規約の情報取り扱いを更新する。

開発者はSNS名の `@Arahabica1` だけを表示し、本名は掲載しない。問い合わせ先も同じSNSアカウントとする。利用規約とクレジットページはシステムフォントで表示する。

## 素材とライセンス

[`THIRD_PARTY_NOTICES.md`](THIRD_PARTY_NOTICES.md)を原典として、利用者が読める日本語のクレジットを `/credits` に掲載する。

- Arbutus Slab
- Kosugi Maru
- M PLUS Rounded 1c
- Bootstrap Iconsを元に調整したスート
- Modianoのダイヤ
- Lucide Icons
- 効果音ラボ
- OtoLogic
- Audiostockの購入素材

素材名、配布元、ライセンス、必要な著作者表示を記載する。購入素材の音源ファイル自体をダウンロード素材として配布せず、アプリ内の効果音としてだけ使用する。

## OGP

### 画像

- サイズは1200×630pxとする。
- 完成したトップページの背景、ロゴカード、「暗算ポーカー」を使う。
- 実際のトップページから「はじめる」を削除せず、OGP画像を作るときだけ非表示にする。
- アニメーションを停止し、フォントの読み込みが完了した状態で撮影する。
- SNSによる切り抜きを考慮し、カードとタイトルを中央の安全な範囲に置く。
- 生成結果を `public/ogp.png` として保存する。

OGP専用の再現可能な撮影状態を用意し、トップページの変更後も同じ手順で画像を作り直せるようにする。サイズと配置は[OGP画像の参考記事](https://bluetraff.com/ogp_thumbnail/)を参考にする。

### メタ情報

トップページに次を設定する。

- titleとdescription
- canonical URL
- `og:title`
- `og:description`
- `og:type=website`
- `og:url=https://azpoker.me/`
- `og:image=https://azpoker.me/ogp.png`
- `og:image:width=1200`
- `og:image:height=630`
- `og:image:alt`
- `twitter:card=summary_large_image`

## 公開前確認

- [ ] `pnpm check` が完走する。
- [ ] 320px、480px、PC幅でトップ、履歴、規約、クレジットを確認する。
- [ ] 各URLをCloudflare Pages上で直接開ける。
- [ ] ブラウザの戻る・進むで画面状態が壊れない。
- [ ] 履歴が結果ごとに1回だけ保存される。
- [ ] 履歴が50件を超えたとき、最も古いものから削除される。
- [ ] LocalStorageが利用できない場合もクイズを完了できる。
- [ ] OGP画像、タイトル、説明、絶対URLを各SNSの確認ツールで検証する。
- [ ] HTTPS、canonical URL、`www`からのリダイレクトを確認する。
- [ ] `robots.txt`と`sitemap.xml`を用意する。
- [ ] 利用規約、素材、開発者、別作品へのリンク切れがない。
- [ ] トップページの静的HTMLと初期表示速度を確認する。
- [ ] Cloudflare Pagesの本番ビルドとデプロイを確認する。

## 実装単位

壊れた中間コミットを作らないため、次の単位で実装し、各単位の最後に `pnpm check` を完走させる。

1. 履歴の型、LocalStorage操作、重複防止とテスト
2. `/history`を含むページ遷移と静的HTML
3. LP本文、最近の履歴、フッター
4. `/terms`と`/credits`
5. OGP撮影、画像、メタ情報、robots、sitemap
6. `azpoker.me`接続後の本番確認

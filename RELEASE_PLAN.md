# 公開準備

暗算ポーカーを `https://azpoker.me/` で公開するまでの作業をまとめる。ゲーム仕様は [`PLAN.md`](PLAN.md)、画面と演出は [`DESIGN.md`](DESIGN.md)、素材の原典とライセンスは [`THIRD_PARTY_NOTICES.md`](THIRD_PARTY_NOTICES.md) を正とする。

## 実装順

1. ページ遷移と履歴保存の基盤
2. トップページのLP化と履歴表示
3. 利用規約、素材、開発者情報
4. OGP画像とメタ情報
5. ドメイン取得・接続
6. 公開前確認

ドメイン取得は実装と並行して進められる。OGP画像は完成したトップページを元にするため、LP確定後に作る。

## ドメイン

- Cloudflare Registrarで `azpoker.me` を取得する。空き状況と価格は購入画面で最終確認する。
- 登録メールの認証、自動更新、DNSSECを確認する。
- 既存のCloudflare Pagesプロジェクトへカスタムドメインとして接続する。
- 正規URLを `https://azpoker.me/` とする。
- `www.azpoker.me` は正規URLへリダイレクトする。
- canonical URLとOGPの絶対URLにも同じドメインを使う。

ドメインの購入とCloudflareアカウント上の最終操作は開発者が行う。

## ページ構成

次のURLを用意し、URLを直接開いた場合やブラウザの戻る・進むでも正しく表示できるようにする。

| URL        | 内容                             |
| ---------- | -------------------------------- |
| `/`        | トップ、LP、クイズ               |
| `/history` | 保存したクイズ結果の一覧         |
| `/terms`   | 利用規約                         |
| `/credits` | 素材、ライセンス、開発者について |

大きなルーターライブラリは追加せず、現在のSvelte構成に合う軽量なページ遷移にする。公開情報を含むページはCloudflare Pagesで直接表示できる静的なHTMLを生成する。

## トップページのLP化

現在のカード、タイトル、「はじめる」をファーストビューに残し、その下へ次の内容を追加する。

### 暗算ポーカーとは

ポーカーで実際に考える確率や勝率を、カードを見ながら2択で答える10問のドリルであることを短く説明する。

### テキサスホールデムとは

各プレイヤーに配られる2枚の手札と、全員が使える5枚の共通カードから、最も強い5枚の組み合わせを作るゲームであることを初心者向けに説明する。

### 出題内容と遊び方

- フラッシュやストレートなどができる確率
- 2つの手札の強さと勝率
- 相手が特定のカードや役を持つ可能性
- 1セット10問、各問題に制限時間があること
- 開始前に音あり・音なしを選べること

### 最近の履歴

保存済みの結果がある場合だけ新しい2件を表示し、「もっと見る」から `/history` へ遷移する。主操作の「はじめる」よりも上で目立たせず、ファーストビューの下に配置する。

### フッター

- 利用規約
- 素材・ライセンス
- 開発者 `@Arahabica1`
- 問い合わせ先 `@Arahabica1`
- 別の作品 [Reaction Maker](https://reaction-maker.com/)

タイトルと「はじめる」には現在のKosugi Maruサブセットを使う。LP本文、履歴、フッターはシステムフォントを使い、トップの初期読み込み用フォントを増やさない。

## 履歴

### 保存

クイズを最後まで完了して結果画面へ移ったときに、次の情報をLocalStorageへ保存する。

- 一意なID
- 完了日時
- 正答数
- 全問題数
- 回答に使った合計時間
- 合計制限時間
- 時間切れ数
- 保存形式のバージョン

保存キーにはアプリ名とバージョンを含め、最大50件を新しい順で保持する。51件目を保存したら最も古い結果を削除する。問題内容や手札など、表示に不要な情報は保存しない。

結果画面への再描画やブラウザ操作による二重保存を防ぐ。LocalStorageが無効、容量不足、または保存値が壊れている場合も、クイズ自体は継続できるようにする。

### トップページ

新しい2件について、日時、正答数、合計時間、時間切れ数を簡潔に表示する。時間切れが0問なら回数を表示しない。履歴がなければ履歴セクション自体を表示しない。

### 履歴一覧

`/history` に最大50件を新しい順で表示する。履歴がない場合は空状態とトップへ戻る導線を表示する。今回の公開範囲には、アカウント同期、サーバー保存、履歴の個別削除は含めない。

## 利用規約

[`reaction-maker.com`](https://reaction-maker.com/)と、その[利用規約](https://reaction-maker.com/terms)の構成を参考に、暗算ポーカー向けの文章を用意する。

- 規約の適用
- サービス内容
- 利用条件と禁止事項
- 知的財産権
- 免責事項
- 情報の保存と取り扱い
- 規約の変更
- 準拠法と管轄
- 問い合わせ先

確率や勝率は学習を目的とした表示であり、実戦結果、利益、特定の判断を保証するものではないことを明記する。履歴は利用者のブラウザのLocalStorageだけに保存し、アカウントやサーバーへ同期しないことも説明する。

アクセス解析などを導入していない段階では、存在しない情報収集について記載しない。将来導入する場合は、公開前に規約の情報取り扱いを更新する。

開発者はSNS名の `@Arahabica1` だけを表示し、本名は掲載しない。問い合わせ先も同じSNSアカウントとする。利用規約とクレジットページはシステムフォントで表示する。

## 素材とライセンス

[`THIRD_PARTY_NOTICES.md`](THIRD_PARTY_NOTICES.md)を原典として、利用者が読める日本語のクレジットを `/credits` に掲載する。

- Arbutus Slab
- Kosugi Maru
- M PLUS Rounded 1c
- Bootstrap Iconsを元に調整したスート
- Modianoのダイヤ
- Lucide Icons
- 効果音ラボ
- OtoLogic
- Audiostockの購入素材

素材名、配布元、ライセンス、必要な著作者表示を記載する。購入素材の音源ファイル自体をダウンロード素材として配布せず、アプリ内の効果音としてだけ使用する。

## OGP

### 画像

- サイズは1200×630pxとする。
- 完成したトップページの背景、ロゴカード、「暗算ポーカー」を使う。
- 実際のトップページから「はじめる」を削除せず、OGP画像を作るときだけ非表示にする。
- アニメーションを停止し、フォントの読み込みが完了した状態で撮影する。
- SNSによる切り抜きを考慮し、カードとタイトルを中央の安全な範囲に置く。
- 生成結果を `public/ogp.png` として保存する。

OGP専用の再現可能な撮影状態を用意し、トップページの変更後も同じ手順で画像を作り直せるようにする。サイズと配置は[OGP画像の参考記事](https://bluetraff.com/ogp_thumbnail/)を参考にする。

### メタ情報

トップページに次を設定する。

- titleとdescription
- canonical URL
- `og:title`
- `og:description`
- `og:type=website`
- `og:url=https://azpoker.me/`
- `og:image=https://azpoker.me/ogp.png`
- `og:image:width=1200`
- `og:image:height=630`
- `og:image:alt`
- `twitter:card=summary_large_image`

## 公開前確認

- [ ] `pnpm check` が完走する。
- [ ] 320px、480px、PC幅でトップ、履歴、規約、クレジットを確認する。
- [ ] 各URLをCloudflare Pages上で直接開ける。
- [ ] ブラウザの戻る・進むで画面状態が壊れない。
- [ ] 履歴が結果ごとに1回だけ保存される。
- [ ] 履歴が50件を超えたとき、最も古いものから削除される。
- [ ] LocalStorageが利用できない場合もクイズを完了できる。
- [ ] OGP画像、タイトル、説明、絶対URLを各SNSの確認ツールで検証する。
- [ ] HTTPS、canonical URL、`www`からのリダイレクトを確認する。
- [ ] `robots.txt`と`sitemap.xml`を用意する。
- [ ] 利用規約、素材、開発者、別作品へのリンク切れがない。
- [ ] トップページの静的HTMLと初期表示速度を確認する。
- [ ] Cloudflare Pagesの本番ビルドとデプロイを確認する。

## 実装単位

壊れた中間コミットを作らないため、次の単位で実装し、各単位の最後に `pnpm check` を完走させる。

1. 履歴の型、LocalStorage操作、重複防止とテスト
2. `/history`を含むページ遷移と静的HTML
3. LP本文、最近の履歴、フッター
4. `/terms`と`/credits`
5. OGP撮影、画像、メタ情報、robots、sitemap
6. `azpoker.me`接続後の本番確認

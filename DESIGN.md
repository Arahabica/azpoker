# 暗算ポーカー デザイン方針

この文書は、現在採用しているデザインの考え方と、再利用可能な視覚・レイアウト・インタラクションの原則をまとめたものです。具体的な表示文言は実装、ゲームルール・確率・問題バンク・実装フェーズは [`PLAN.md`](PLAN.md)、公開準備は [`RELEASE_PLAN.md`](RELEASE_PLAN.md)、起動方法とコード構成は [`README.md`](README.md) を正とします。

この文書にはデザイン判断の拠り所になる原則だけを記載し、方向性が変わったときに更新します。

## 1. デザイン方針

目指す雰囲気は、**クラシック寄りでありながら、親しみやすさとポップさもあるデザイン**です。

- 緑のフェルト、温かい紙色のカード、セリフ体のカードランクでクラシックさを作る。
- 丸ゴシック、明るい黄色、大きめの角丸で親しみやすさとポップさを加える。
- 重厚で高級なカジノの雰囲気だけには寄せず、気軽に繰り返せるドリルとしての軽さを保つ。
- モバイルで、ポーカーの卓を見ながら暗算する体験に集中させる。
- ユーザーの判断に必要な情報だけを表示し、ステージ名や説明用ラベルを増やさない。
- カード、余白、文字、操作のルールを全画面で統一し、公開サービスとしてトップ・問題・結果の画面状態を明確に分ける。
- 装飾はポーカーらしさと視認性に寄与する範囲に留める。

### デザインの4原則

| 原則 | このアプリでの適用                                                                                           |
| ---- | ------------------------------------------------------------------------------------------------------------ |
| 近接 | ボード、手札、選択肢、回答内容をそれぞれまとまりとして配置する。関連しないまとまりの間には十分な余白を取る。 |
| 整列 | ボードは5列グリッド、カード左上は共通中心軸、選択肢内の補助語・数値・単位は共通ベースラインへ揃える。        |
| 反復 | トップと問題画面で同じカード面、同じスート、同じ色・角丸・影のルールを使う。                                 |
| 対比 | 緑のフェルト、明るいカード面、黄色の主要操作で優先度を分ける。補助情報は彩度とサイズを抑える。               |

## 2. 表示領域とレスポンシブ

- モバイルを基準とし、対応幅は320px以上、アプリ領域は `max-width: 480px` とする。
- PCでもアプリを引き伸ばさず中央へ置き、左右は暗い余白にする。481px以上ではアプリ境界に細い線と影を付ける。
- 各画面は `100dvh` を基準にし、上端・下端のセーフエリアを考慮する。
- 基本の左右余白は `clamp(1rem, 5vw, 1.5rem)` とする。
- LPのヒーローと本文の画面端からの左右余白は `--lp-padding-horizontal` に統一する。カードや説明パネルなど、コンポーネント内部の余白とは分けて管理する。
- 高さ620px以下では、問題文の余白、カード間隔、回答パネルを詰める。横スクロールは発生させない。

## 3. 情報設計と画面構成

トップ、開始準備、問題、結果は独立した画面状態として扱います。履歴、利用規約、素材・開発者情報は固有のURLを持つ公開ページとします。初回アクセスで問題を自動開始しません。

### トップ

- 初期レスポンスにトップ画面のHTMLを含め、JavaScriptの実行前から内容とレイアウトを表示する。ブラウザでは同じDOMをhydrateする。
- ファーストビューにはブランド名、短い価値提案、主要な開始操作だけを置き、サービスの目的とクイズ形式を最初の画面で判断できるようにする。
- ロゴにはスペードのAとハートの10を使い、`LogoCards.svelte` でカードを重ねて傾ける。回転は左 `-9deg`、右 `8deg` とする。
- タイトル、サブタイトル、カードを中央のまとまりにし、開始操作は画面下部の主要操作として分離する。
- ヒーローは`100dvh`を使い、端末のファーストビュー全体を落ち着いて使う。
- 履歴が一定数ある場合だけ、ヒーロー内で開始操作の直前に直近の結果を表示する。各結果は相対時刻・正答数・回答時間を1行に圧縮し、見出しと一覧への導線は控えめにする。履歴が少ない場合は領域自体を描画しない。
- ヒーローの次は実際の問題だけで`100dvh`を使い、利用者が説明を読む前にまず自分で考えられるようにする。ボードと手札の間、カード群と選択肢の間には明確な余白を置く。
- 問題例の直後は、概算値と詳細値に明確な文字サイズの差を付けて答えを示す。表示値は確率エンジンの計算結果と必ず一致させる。
- 答えの後は、体験の価値、クイズの概要、継続利用の利点を同じ本文スタイルで短く説明する。番号付きパネルや細かな機能一覧は置かない。
- 初心者向けの導入からルール説明へ自然につなぐ。説明では共通カード5枚を上、手札2枚を下に実物のカード面で図示し、詳細なルールは信頼できる外部ページへ案内する。
- フッターの前に開始操作をもう一度置き、説明を読んだ位置からすぐに行動できるようにする。
- ヒーローとLP本文は別のKosugi Maruサブセットを使う。どちらも英数字だけM PLUS Rounded 1cを使い、ヒーロー用Kosugiだけを初期HTMLから先読みする。

### 履歴・公開情報

- `/history` は完了からの経過時間、正答数、回答時間を新しい順で最大50件表示する。履歴がない場合は空状態と挑戦する操作を置く。
- 履歴の行は `/history/{履歴ID}` の詳細への操作にする。一覧は `history/index.html`、詳細はクライアント描画用の `history-detail/index.html` に分け、詳細URLでは専用HTMLを返す。履歴IDと内容はブラウザ内で生成・保存されるため、IDごとのHTMLは生成しない。詳細では問題画面と同じボード・手札を使い、1セットの全問題を縦に並べる。各問題へ難易度、正誤、選択回答、正解、実際の値、解説を表示する。選択回答と正解は選択肢または手札上で示し、同じ値の要約を重ねて表示しない。
- 詳細上部の戻り導線は1つだけとし、トップの履歴から開いた場合は「トップへ」、履歴一覧から開いた場合は「履歴一覧へ戻る」とする。詳細URLを直接開いた場合は履歴一覧を戻り先にする。
- `/terms` と `/credits` は読みやすさを優先してシステムフォントを使い、見出しと本文を暗い半透明パネルへまとめる。
- 公開ページは左上のトップへの導線、下部の共通フッター、ブラウザの戻る・進むで移動できるようにする。
- 共通フッターは、利用者への呼びかけ、開発者への連絡手段、法的情報とクレジットへの導線の3層にする。

### 開始準備

- トップの主要操作を押すと画面要素をすぐに消し、安定して描画済みのフェルト背景だけを残して問題、ゲーム用フォント、効果音を先読みする。
- 遷移中の表示はちらつき防止の共通方針に従い、読み込み完了後に開始準備画面を表示する。
- 画面下寄りに音あり・音なしを切り替える円形のスピーカー操作を置き、現在の状態をLucideの`Volume2`・`VolumeX`と文字の両方で示す。初期状態は音ありとする。主要操作と競合しない灰緑色を使い、輪郭線は付けず、下側の影と押下時の沈み込みで立体的なボタンにする。
- 画面下部に黄色の主要操作を置く。準備画面は読み込み完了後の確定した状態で初めて表示し、中間状態を説明する補助文字は置かない。
- 読み込み失敗時は、200msで表示した準備画面にエラーと再試行操作を表示する。

### 問題

上から次の順に配置します。

1. 進捗・残り時間を統合したセグメント
2. 問題文
3. ボード
4. 手札
5. 2つの選択肢

- 数字による進捗、ストリート名、カード領域の説明ラベル、補足説明は表示せず、位置とセグメントだけで構造を伝える。
- セット内の全問題を同じ太さのセグメントで表す。通常の基本10問に履歴から1問追加される場合や、復習で問題がキュー末尾へ戻る場合も問題数へ追従する。現在セグメントの横幅は制限時間の5・8・12・16秒に比例させ、外形はカウント中に動かさない。
- 現在セグメントには秒数を表示せず、内側の白いゲージを左から右へ伸ばす。回答済みは正解を緑、不正解と時間切れを赤で残す。
- 残り時間は問題へ回答できるようになってから表示・更新する。残り3秒から画面全体へ赤い点滅を重ね、残り1.5秒では点滅を速く強くする。
- ボードを上、手札を下に置く。実際の卓と同じ認知順序にする。
- 選択肢は横2列とし、問題表示の最初からDOMと高さを確保する。待機中と回答後は `display: none` にせず、不可視のまま場所を維持する。
- モードBは共通ボードの下へ2組の手札を横並びに置く。手札選択型では手札そのものを選択肢にし、番号などの視覚ラベルは追加しない。数値で勝率を尋ねる対象は必ず右側へ配置し、右の手札の枠を黄色で強調する。
- モードCは通常の手札と確率選択肢を使い、問題文だけで2人・6人を区別する。
- 問題画面上部に常設の戻るアイコンは置かない。

### 回答

- 卓とカードを残したまま、画面下から濃色の回答パネルを出す。
- 表示するのは正誤、実際の値、短い解説、次へ進む操作、右上の目立たない退出アイコンだけにする。
- 実際の値を最も強く見せ、黄色の強調色を使う。
- 退出アイコンは開いたドアと外向きの矢印で退出を表し、同じ下部パネル形式の確認を重ねる。退出と継続の操作を縦に並べ、継続を黄色の主要操作、退出を赤系の控えめな操作にする。

### 結果

- 正答数と回答に使った合計時間を主要情報として表示し、総問題数と合計制限時間は一段小さい補助情報にする。時間切れは発生した場合だけ追加する。
- 結果の評価は正確さを優先し、高い正答率かつ時間切れがない場合だけ速度も肯定的に扱う。
- 全問正解では王冠、黄色の光、紙吹雪を加え、通常の結果より明確に派手にする。`prefers-reduced-motion`では紙吹雪を非表示にする。
- 通常結果の操作は「問題を続ける」、誤答がある場合だけその下の「復習する」、最後に「トップページに戻る」の順にする。全問正解では復習操作を表示しない。

### 復習

- 復習中は進捗の上へ「復習」と難易度を表示し、通常出題との違いを明確にする。卓、選択肢、回答パネルは通常問題と同じ部品を使う。
- 再び間違えた問題はキューの最後へ移し、別の問題を挟んだ後に再出題する。1問だけの場合は次の問題として再出題する。
- 完了画面は通常結果と同じ全画面構成を使い、10種類以上からランダムに選ぶ短い労い文言、完了マーク、「問題を続ける」「トップページに戻る」だけを表示する。説明文や英字ラベルは置かない。
- 再挑戦を主要操作、トップへ戻る導線を副操作にする。
- 平均秒数は表示しない。制限時間インジケーターは問題画面にだけ表示する。

### 効果音

- スタート、正解、不正解・時間切れ、時間警告、通常結果、全問正解に短い効果音を使う。
- Web Audio APIで準備画面中に全効果音をデコードし、回答時は保存済みの`AudioBuffer`から即時再生する。
- 効果音は視覚的な正誤・結果表示を補助するものとし、音だけで状態を伝えない。
- 音なしを選んだ場合は、セッション中のすべての効果音を再生しない。

## 4. レイアウト

### ボードと手札

- ボード領域は常にカード5枚分のグリッドを確保する。
- プリフロップは空間だけ、フロップは左から3枚、ターンは左から4枚を表示する。
- 未表示カードを示す点線枠は置かない。空いている列そのものを残り枚数の手がかりにする。
- 手札2枚は中央で少し重ね、左 `-6deg`、右 `6deg` に傾ける。
- カード領域の説明ラベルは置かず、位置関係だけで伝える。

### 選択肢

- 各ボタンは最低4remの高さを持つ。
- 内容は `inline-flex` でベースラインを揃え、概算を示す補助語・数値・単位を別要素にする。
- 補助語と単位は同じ大きさ、数値は一段大きくする。
- 現在の上下余白は `0.5rem 0.75rem 0.66rem` とし、幾何学的な等間隔ではなく見た目の中央を優先する。
- 選択肢面はフェルトより暗い半透明色にし、主要操作の黄色とは競合させない。

## 5. カラー

アクセントカラーは次の黄色に統一します。

| 用途               | 値                   |
| ------------------ | -------------------- |
| 画面外             | `#020b09`            |
| フェルト           | `#08745e`            |
| フェルト暗部       | `#055544`            |
| フェルト明部       | `#0b8a6e`            |
| 基本文字           | `#f7faf8`            |
| 補助文字           | `#a9c2b9`            |
| アクセント         | `rgb(241, 196, 15)`  |
| アクセント・ホバー | `#f6d137`            |
| アクセント・押下影 | `#d7ad0d`            |
| アクセント上の文字 | `#29220d`            |
| カード面           | `#f8f5ec`            |
| カード面の陰影     | `#e7e2d7`            |
| 選択肢面           | `rgb(2 33 25 / 68%)` |
| 回答パネル         | `#11211d`            |
| 黒スート・ランク   | `#111719`            |
| 赤スート・ランク   | `#c82d3c`            |
| 正解               | `#20ca91`            |
| 不正解             | `#ff6b6f`            |

- フェルトには弱い繊維状の反復パターン、中央の光、周辺の暗部を重ねる。
- 主要ボタン、進捗、回答値だけに黄色を反復して使う。
- カード面は純白にせず、わずかに温かい紙色と陰影を使う。

## 6. タイポグラフィ

フォントはすべて自己配信し、マシンの環境差や外部CDNへ依存しません。

| 対象                     | フォント                  | ルール                                                                                                                      |
| ------------------------ | ------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| ヒーローの日本語         | Kosugi Maru               | タイトル、サブタイトル、開始操作、最近の履歴に必要な文字だけを収録した初期表示用サブセットを使い、HTMLから先読みする。      |
| LP本文の日本語           | Kosugi Maru               | ヒーローとは別の本文用サブセットを使う。                                                                                    |
| 履歴一覧・公開情報       | システムフォント          | 一覧と長文の可読性を優先する。履歴の問題詳細だけはゲーム用Kosugi Maruを使う。                                               |
| 問題・回答・結果の日本語 | Kosugi Maru               | ゲーム開始後に読み込むサブセットを使う。                                                                                    |
| UIの英数字               | M PLUS Rounded 1c Regular | LPとゲームの英字・数字・小数点に使う。`MixedFontText.svelte`では周囲の太字を継承させず400で表示し、`%`はKosugi Maruに残す。 |
| カードランク             | Arbutus Slab              | カード面だけに使用する。                                                                                                    |

- 問題文は中央揃えにし、条件を短く一読できる表現にする。意味のまとまりは改行不可とし、まとまりの間にだけ `<wbr>` を置いて不自然な改行を防ぐ。
- 選択肢の数値、回答値、スコアは400とし、数字の字形を変えない。
- ヒーロー、LP本文、ゲーム画面の日本語はKosugi Maruで一貫させ、LP本文だけを別ファイルに分ける。履歴一覧と公開情報はシステムフォントで読みやすくする。
- フォントの収録文字と再生成方法は [`assets/fonts/README.md`](assets/fonts/README.md) を参照する。

## 7. カード

### 共通ルール

- トップ、ボード、手札のカード面はすべて `PlayingCard.svelte` と `CardFace.svelte` を使う。
- `PlayingCard.svelte` はカードの描画と読み上げだけを担当し、ボード、手札、ロゴの利用文脈を知らない。
- サイズ、重なり、角度、モーションは `Board.svelte`、`HoleCards.svelte`、`LogoCards.svelte` がそれぞれ担当する。
- 縦横比は `5 / 7`。紙色のグラデーション、細い枠、内側のハイライト、卓上の影を持たせる。
- 10は `T` と省略せず、常に `10` と表示・読み上げる。
- 絵札に人物画は使わず、`J / Q / K` とスートで統一する。
- ハートとダイヤは赤、スペードとクラブは黒にする。
- スートはシンプルでクラシック、かつ輪郭にシャープさのある形を使う。

### ランクとスートの整列

- 左上のランクと小スートは、カード幅の22%に置いた共通中心軸へ個別に揃える。
- ランク文字の幅に小スートを追従させず、すべてのランクで小スートの水平位置を固定する。
- ランクは34cqi、小スートは19cqiを基準にする。
- 中央スートはカード幅の52%、中心位置は横50%・縦62%とする。
- Arbutus Slabの視覚中心だけをランク側で補正し、Qは左へ1.5cqi、Kは左へ2cqi移動する。

## 8. モーションとアクセシビリティ

### ちらつき防止

- 非同期処理を伴う画面遷移では、データやアセットが未確定の遷移先画面を一度表示しない。読み込み中は描画済みの背景と外枠だけを残し、レイアウトが変化する要素を置かない。
- 読み込みが350ms未満ならローディング表示を挟まず、そのまま遷移先を表示する。350ms以上かかる場合だけ背景中央にローディングアニメーションを表示する。
- 一度表示したローディングは最低600ms維持し、短時間だけ現れて消えるちらつきを防ぐ。処理がそれ以上かかる場合は、実際の完了まで表示を続ける。
- 読み込み完了後に、確定した遷移先画面全体を200msでフェードインする。アニメーションは不透明度だけを変え、位置・サイズ・レイアウトを動かさない。
- 読み込み失敗も処理完了として扱い、未完成の画面ではなく、確定したエラー表示と再試行操作をフェードインする。
- 参考: [ローディングアニメーションのちらつきを防ぐ方法](https://zenn.dev/voiceapplab/articles/8a8d60234b51b5)

### 画面内のモーション

- ボードと手札は350ms、ロゴカードは300msの `ease-in-out` モーションを使い、遅延なしで同時に始める。
- ボードと手札は透明度 `0.4`、ロゴカードは透明度 `0.6` で最初から配置し、下4pxから本来の位置へ移動しながら透明度 `1` にする。サイズは変えない。
- フロップ問題は3枚、ターン問題は4枚を同時に配置する。ターンだけを後から追加しない。
- 手札は左 `-3deg` から `-6deg`、右 `3deg` から `6deg` へ、3degだけ扇状に開く。ロゴは逆に、開始角度から最終角度へわずかに閉じる。2枚目だけを遅らせない。
- 選択肢は300ms後に可視化するが、DOMと領域は最初のフレームから確保する。
- 回答パネルは下から280msで表示する。次の問題では横長の現在セグメントを約200msで切り替え、ヘッダーの高さは変えない。
- 制限時間インジケーターは警告中も白い進捗と白い発光を保つ。残り3秒で画面全体の警告を始め、残り2.2秒から回答または時間切れまで警告音を繰り返し、残り1.5秒で発光をさらに強くする。時間切れ後は動きを止める。
- `prefers-reduced-motion: reduce` では待ち時間を0にし、アニメーションとトランジションを実質無効にする。
- キーボードフォーカスは白い3pxのアウトラインで明示する。
- 視覚ラベルを置かないコミュニティカード、手札、数字の進捗はARIAラベルで補う。
- ロゴカードは装飾として読み上げ対象から外し、問題中のカードにはランクとスートの読み上げ名を持たせる。
- 主要ボタンは最低3.7rem、選択肢は最低4rem、戻る操作は2.75rem四方を確保する。

## 9. 実装・確認ルール

- ゲーム画面は `LandingScreen.svelte`、`PreparationLoadingScreen.svelte`、`PrepareScreen.svelte`、`QuizScreen.svelte`、`ResultScreen.svelte`、`ReviewResultScreen.svelte` に分ける。履歴、履歴問題詳細、利用規約、素材・開発者情報も独立した画面コンポーネントにする。
- ゲーム進行は `game-flow.ts` の純粋なTypeScript状態機械で管理する。問題番号と回答結果は進行状態に含め、Svelte側に重複した状態を作らない。問題取得、フォーカス、効果音などの副作用だけを `App.svelte` から実行する。
- ボード、手札、ロゴカード、カード枠、カード面、操作ボタン、選択肢、回答パネルはそれぞれ共通コンポーネントを使い、画面ごとの複製を作らない。
- コンポーネント固有のレイアウト、装飾、モーションは各 `.svelte` のスコープ付き `<style>` に置く。
- `styles.css` にはフォント定義、デザイントークン、リセット、全体のアクセシビリティ設定だけを置く。
- 本番コードには、実際に使用する画面、コンポーネント、スタイルだけを置く。
- 外部画像、外部フォントCDN、CSSフレームワークは使わない。
- 変更後は最低でも320px、480px、481px以上のPC幅、高さ620px以下を確認する。
- カードの変更は、幅の異なる代表例としてA、5、10、J、Q、Kを使い、小スートの軸と余白を確認する。
- モーション変更は通常設定と `prefers-reduced-motion` の両方を確認する。
- コミット前に `pnpm check` を完走させる。

# 暗算ポーカー デザイン仕様

この文書は、現在採用しているビジュアル、画面構成、インタラクション、表示文言の基準をまとめたものです。ゲームルール、確率、問題バンク、実装フェーズは [`PLAN.md`](PLAN.md)、起動方法とコード構成は [`README.md`](README.md) を正とします。

この文書には現在使っている仕様だけを記載し、デザイン変更時は実装と同時に更新します。

## 1. デザイン方針

目指す雰囲気は、**クラシック寄りでありながら、親しみやすさとポップさもあるデザイン**です。

- 緑のフェルト、温かい紙色のカード、セリフ体のカードランクでクラシックさを作る。
- 丸ゴシック、明るい黄色、大きめの角丸で親しみやすさとポップさを加える。
- 重厚で高級なカジノの雰囲気だけには寄せず、気軽に繰り返せるドリルとしての軽さを保つ。
- モバイルで、ポーカーの卓を見ながら暗算する体験に集中させる。
- ユーザーの判断に必要な情報だけを表示し、ステージ名や説明用ラベルを増やさない。
- カード、余白、文字、操作のルールを全画面で統一し、公開サービスとしてトップ・問題・結果の画面状態を明確に分ける。
- 装飾はポーカーらしさと視認性に寄与する範囲に留める。

### デザインの4原則

| 原則 | このアプリでの適用 |
|---|---|
| 近接 | ボード、手札、選択肢、回答内容をそれぞれまとまりとして配置する。関連しないまとまりの間には十分な余白を取る。 |
| 整列 | ボードは5列グリッド、カード左上は共通中心軸、選択肢内の「約」・数値・`%`は共通ベースラインへ揃える。 |
| 反復 | トップと問題画面で同じカード面、同じスート、同じ色・角丸・影のルールを使う。 |
| 対比 | 緑のフェルト、明るいカード面、黄色の主要操作で優先度を分ける。補助情報は彩度とサイズを抑える。 |

## 2. 表示領域とレスポンシブ

- モバイルを基準とし、対応幅は320px以上、アプリ領域は `max-width: 480px` とする。
- PCでもアプリを引き伸ばさず中央へ置き、左右は暗い余白にする。481px以上ではアプリ境界に細い線と影を付ける。
- 各画面は `100dvh` を基準にし、上端・下端のセーフエリアを考慮する。
- 基本の左右余白は `clamp(1rem, 5vw, 1.5rem)` とする。
- 高さ620px以下では、問題文の余白、カード間隔、回答パネルを詰める。横スクロールは発生させない。

## 3. 画面構成

トップ、問題、結果は独立した画面状態として扱います。初回アクセスで問題を自動開始しません。

### トップ

- 初期レスポンスにトップ画面のHTMLを含め、JavaScriptの実行前から内容とレイアウトを表示する。ブラウザでは同じDOMをhydrateする。
- 表示する文字は「暗算ポーカー」と「はじめる」だけにする。
- ロゴにはスペードのAとハートの10を使い、`LogoCards.svelte` でカードを重ねて傾ける。回転は左 `-9deg`、右 `8deg` とする。
- タイトルとカードを中央のまとまりにし、「はじめる」は画面下部の主要操作として分離する。

### 問題

上から次の順に配置します。

1. 戻るアイコンと進捗バー
2. 問題文
3. ボード
4. 手札
5. 2つの選択肢

- 数字の進捗、`FLOP`、`TURN`、`ボード`、補足説明は表示しない。
- ボードを上、手札を下に置く。実際の卓と同じ認知順序にする。
- 選択肢は横2列とし、問題表示の最初からDOMと高さを確保する。待機中と回答後は `display: none` にせず、不可視のまま場所を維持する。
- モードBは共通ボードの下へ2組の手札を横並びに置き、手札そのものを選択肢にする。番号や「手札1」のような視覚ラベルは追加しない。
- モードCは通常の手札と確率選択肢を使い、問題文だけで2人・6人を区別する。

### 回答

- 卓とカードを残したまま、画面下から濃色の回答パネルを出す。
- 表示するのは正誤、実際の値、短い解説、次へ進む操作だけにする。
- 実際の値を最も強く見せ、黄色の強調色を使う。

### 結果

- 正解数を画面の中心へ大きく表示する。
- 主要操作は「もう一度」、副操作は「トップへ」とする。
- 平均秒数、回答時間、タイマーは表示しない。

## 4. レイアウト

### ボードと手札

- ボード領域は常にカード5枚分のグリッドを確保する。
- プリフロップは空間だけ、フロップは左から3枚、ターンは左から4枚を表示する。
- 未表示カードを示す点線枠は置かない。空いている列そのものを残り枚数の手がかりにする。
- 手札2枚は中央で少し重ね、左 `-6deg`、右 `6deg` に傾ける。
- 「手札」「ボード」という表示ラベルは置かず、カードの位置関係だけで伝える。

### 選択肢

- 各ボタンは最低4remの高さを持つ。
- 内容は `inline-flex` でベースラインを揃え、「約」・数値・`%`を別要素にする。
- 「約」と `%` は同じ大きさ、数値は一段大きくする。
- 現在の上下余白は `0.5rem 0.75rem 0.66rem` とし、幾何学的な等間隔ではなく見た目の中央を優先する。
- 選択肢面はフェルトより暗い半透明色にし、主要操作の黄色とは競合させない。

## 5. カラー

アクセントカラーは次の黄色に統一します。

| 用途 | 値 |
|---|---|
| 画面外 | `#020b09` |
| フェルト | `#08745e` |
| フェルト暗部 | `#055544` |
| フェルト明部 | `#0b8a6e` |
| 基本文字 | `#f7faf8` |
| 補助文字 | `#a9c2b9` |
| アクセント | `rgb(241, 196, 15)` |
| アクセント・ホバー | `#f6d137` |
| アクセント・押下影 | `#d7ad0d` |
| アクセント上の文字 | `#29220d` |
| カード面 | `#f8f5ec` |
| カード面の陰影 | `#e7e2d7` |
| 選択肢面 | `rgb(2 33 25 / 68%)` |
| 回答パネル | `#11211d` |
| 黒スート・ランク | `#111719` |
| 赤スート・ランク | `#c82d3c` |
| 正解 | `#20ca91` |
| 不正解 | `#ff6b6f` |

- フェルトには弱い繊維状の反復パターン、中央の光、周辺の暗部を重ねる。
- 主要ボタン、進捗、回答値だけに黄色を反復して使う。
- カード面は純白にせず、わずかに温かい紙色と陰影を使う。

## 6. タイポグラフィ

フォントはすべて自己配信し、マシンの環境差や外部CDNへ依存しません。

| 対象 | フォント | ルール |
|---|---|---|
| トップの日本語 | Kosugi Maru | 「暗算ポーカー」「はじめる」の固有9文字だけを収録した初期表示用サブセットを使い、HTMLから先読みする。 |
| 問題・回答・結果の日本語 | Kosugi Maru | ゲーム開始後に読み込むサブセットを使う。 |
| UIの英数字と `%` | M PLUS Rounded 1c Regular | `MixedFontText.svelte`で文字単位に分け、周囲の太字を継承させず400で表示する。 |
| カードランク | Arbutus Slab | カード面だけに使用する。 |

- 問題文は中央揃えで、短いまとまりとして読める行幅にする。
- 選択肢の数値、回答値、スコアは400とし、数字の字形を変えない。
- カード以外の日本語は丸ゴシックの一貫性を保つ。
- フォントの収録文字と再生成方法は [`assets/fonts/README.md`](assets/fonts/README.md) を参照する。

## 7. カード

### 共通ルール

- トップ、ボード、手札のカード面はすべて `PlayingCard.svelte` と `CardFace.svelte` を使う。
- `PlayingCard.svelte` はカードの描画と読み上げだけを担当し、ボード、手札、ロゴの利用文脈を知らない。
- サイズ、重なり、角度、モーションは `Board.svelte`、`HoleCards.svelte`、`LogoCards.svelte` がそれぞれ担当する。
- 縦横比は `5 / 7`。紙色のグラデーション、細い枠、内側のハイライト、卓上の影を持たせる。
- 10は `T` と省略せず、常に `10` と表示・読み上げる。
- 絵札に人物画は使わず、`J / Q / K` とスートで統一する。
- ハートとダイヤは赤、スペードとクラブは黒にする。
- スートはシンプルでクラシック、かつ輪郭にシャープさのある形を使う。

### ランクとスートの整列

- 左上のランクと小スートは、カード幅の22%に置いた共通中心軸へ個別に揃える。
- ランク文字の幅に小スートを追従させず、すべてのランクで小スートの水平位置を固定する。
- ランクは34cqi、小スートは19cqiを基準にする。
- 中央スートはカード幅の52%、中心位置は横50%・縦62%とする。
- Arbutus Slabの視覚中心だけをランク側で補正し、Qは左へ1.5cqi、Kは左へ2cqi移動する。

## 8. 表示文言

- 問題文は対象だけを短く書く。「フラッシュの確率は？」「Aが出る確率は？」「Aがスリーになる確率は？」「どちらが強い？」「6人での勝率は？」「ほかの誰かがKを持つ確率は？」を基準にする。
- 「完成」「リバーまでに」「近いのは」「厳密値」「実際は約」は付けない。
- 「Aが出る確率」はAを手札に持ち、Aが来ることがハンドの改善になる場合だけ使う。他のランクも同じルールとする。
- コンボドローは「フラッシュかストレートの確率は？」と書き、対象を曖昧にしない。
- 選択肢には小さな「約」を付ける。低確率では1%・2.5%、それ以上では2.5%・5%単位を使い、問題の面白さに合う精度を選ぶ。
- 誤答は同じ人数・ステージ・完成条件で起こる典型的な勘違いから作り、常識だけで消せる値や別条件の正解値を置かない。
- 回答後は `31.5%` のように小数1桁の値だけを大きく表示する。
- 解説は次に思い出せる比較を書く。専門用語は「両端のどちらでも完成する形（OESD）」のように日本語の意味を先に示す。「全組合せを数えると」「全列挙では」など、実装や計算作業は見せない。

## 9. モーションとアクセシビリティ

- ボードと手札は350ms、ロゴカードは300msの `ease-in-out` モーションを使い、遅延なしで同時に始める。
- ボードと手札は透明度 `0.4`、ロゴカードは透明度 `0.6` で最初から配置し、下4pxから本来の位置へ移動しながら透明度 `1` にする。サイズは変えない。
- フロップ問題は3枚、ターン問題は4枚を同時に配置する。ターンだけを後から追加しない。
- 手札は左 `-3deg` から `-6deg`、右 `3deg` から `6deg` へ、3degだけ扇状に開く。ロゴは逆に、開始角度から最終角度へわずかに閉じる。2枚目だけを遅らせない。
- 選択肢は300ms後に可視化するが、DOMと領域は最初のフレームから確保する。
- 回答パネルは下から280msで表示し、進捗バーは260msで更新する。
- `prefers-reduced-motion: reduce` では待ち時間を0にし、アニメーションとトランジションを実質無効にする。
- キーボードフォーカスは白い3pxのアウトラインで明示する。
- 視覚ラベルを置かないコミュニティカード、手札、数字の進捗はARIAラベルで補う。
- ロゴカードは装飾として読み上げ対象から外し、問題中のカードにはランクとスートの読み上げ名を持たせる。
- 主要ボタンは最低3.7rem、選択肢は最低4rem、戻る操作は2.75rem四方を確保する。

## 10. 実装・確認ルール

- 画面は `LandingScreen.svelte`、`QuizScreen.svelte`、`ResultScreen.svelte` に分ける。
- ボード、手札、ロゴカード、カード枠、カード面、操作ボタン、選択肢、回答パネルはそれぞれ共通コンポーネントを使い、画面ごとの複製を作らない。
- コンポーネント固有のレイアウト、装飾、モーションは各 `.svelte` のスコープ付き `<style>` に置く。
- `styles.css` にはフォント定義、デザイントークン、リセット、全体のアクセシビリティ設定だけを置く。
- 本番コードには、実際に使用する画面、コンポーネント、スタイルだけを置く。
- 外部画像、外部フォントCDN、CSSフレームワークは使わない。
- 変更後は最低でも320px、480px、481px以上のPC幅、高さ620px以下を確認する。
- カードの変更は、幅の異なる代表例としてA、5、10、J、Q、Kを使い、小スートの軸と余白を確認する。
- モーション変更は通常設定と `prefers-reduced-motion` の両方を確認する。
- コミット前に `pnpm check` を完走させる。

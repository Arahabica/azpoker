{
  "name": "anzan-poker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "暗算ポーカー｜勝率と確率のドリル",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check:bank": "python3 scripts/generate_question_bank.py --check",
    "check": "npm test && npm run check:bank && npm run build",
    "fonts:build": "node scripts/build_font_subsets.mjs"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.1.1",
    "svelte": "^5.56.8",
    "vite": "^6.4.3"
  }
}

# 暗算ポーカー 仕様

**暗算ポーカー｜勝率と確率のドリル**

ポーカーで実際に考える確率を、カードを見ながら2択で答える。画面と文言は [`DESIGN.md`](DESIGN.md)、起動とコード構成は [`README.md`](README.md) を正とする。

## セッション

| 項目 | 内容 |
|---|---|
| 1セット | 10問、制限時間・時間計測なし |
| モード | A 5問、B 2問、C 1問、D 2問 |
| ステージ | プリフロップ3問、フロップ4問、ターン3問 |
| 考え方 | 1原則で判断する問題7問、複数要素を組み合わせる問題3問 |
| 0% | 一見可能そうな0%問題は最大1問 |
| 重複 | Aのカテゴリは重複させず、直近500問を再出題しない |

## 問題バンク

問題は生成時に確率と勝敗を計算し、本番では100問単位のJSONから必要な番号だけを取得する。初期JavaScriptへ問題本体を含めない。

| モード | 問題数 | 内容 |
|---|---:|---|
| A | 6,000 | 役・ランクが成立する確率 |
| B | 3,000 | 2つの手札の勝率比較 |
| C | 338 | 169初期ハンド × 2人・6人 |
| D | 662 | 相手が指定ランクを持つ確率 |
| 合計 | 10,000 | JSON 101ファイル |

### モードA

| カテゴリ | 問題数 |
|---|---:|
| フラッシュ | 1,400 |
| ストレート | 1,600 |
| フラッシュまたはストレート | 800 |
| 指定ランクが出る | 550 |
| 指定ランクがスリーになる | 500 |
| ツーペア | 400 |
| フルハウス | 400 |
| フォーカード | 250 |
| ストレートフラッシュ | 100 |

通常のドロー、バックドア、ランナーランナー、重複アウツ、残り枚数不足による0%を含める。すでに対象が完成している状況は出さない。

### モードB

プリフロップ700問、フロップ1,150問、ターン1,150問。ポケットペア、オーバーカード、ドミネート、完成役対ドロー、キッカー、カウンターフィットなどを扱う。51〜55%の接戦も、複数要素が競合する難問として制限せずに出す。勝率の高さだけでは除外せず、完成役に明確な差があり、有効なドローやキッカー勝負もないなど、判断材料が1つしかない問題を除外する。モンテカルロの信頼区間が50%をまたぐプリフロップ候補だけは正解が不安定なため除外する。

### モードC

169種類の初期ハンドを2人・6人で1問ずつ用意する。スート違いで水増しせず、別人数の勝率を誤答へ流用しない。例えばA♥5♥・2人は約60%対約45%とする。

### モードD

2人では「相手がKを持つ確率」、6人では「ほかの誰かがKを持つ確率」と問う。2〜Aの全ランクを対象にし、見えていない同ランクの枚数と、相手へ配られる2枚または10枚から組合せで求める。ハンドレンジやアクションは考慮しない。

## 選択肢

概算値は `0 / 1 / 2.5 / 5 / 7.5 / 10 / 12.5 / 15 / 17.5 / 20 / 25 … 100%` から、実際の値に最も近いものを使う。回答後は小数1桁を表示する。

誤答には必ず `distractorModel` を持たせ、次のような典型的な勘違いから作る。

- 残り1枚と2枚を混同する
- 重複アウツを二重に数える
- バックドアを通常ドローとして数える
- 見えている同ランクを引き忘れる
- 6人卓で相手1人分しか考えない
- キッカー、完成役、強いドローの一方を過大評価する

数値差だけで難しくしたり、別人数・別ステージの値を捨て選択肢として流用したりしない。

## 計算と生成

- Aは残りカードの全列挙。
- Bのフロップ・ターンは完全な7枚役評価と全列挙。
- Bのプリフロップは12,000試行、Cは30,000試行の固定seedモンテカルロ。Cはハンド単位で並列計算する。
- Dは超幾何分布。
- `pnpm questions:build` で全生成し、`pnpm check:bank` は生成済み10,000問の構造とmanifestを高速検査する。
- 同型判定ではスートを初出順に正規化し、スート名だけを置換した問題を重複させない。

## 初心者向け文言

「3カード」は使わず「スリー」とする。専門用語を使う場合は、日本語の意味を先に書く。

- 両端のどちらでも完成する形（OESD）
- 内側の1ランクだけを待つ形（ガットショット）
- 内側の待ちが2種類ある形（ダブルガット）
- 残り2枚が両方そろって完成する形（ランナーランナー）
- 複数の役を同時に待つ形（コンボドロー）

## 完了条件

- 10,000問とモード・カテゴリ別件数が一致する。
- 100問単位のJSON、manifest、ID、同型除外キーが整合する。
- 全問に有効なカード、正解、誤答理由、難易度がある。
- 10問が固定モード・ステージ・考え方比率、0%・接戦上限を満たす。
- JSON取得失敗から再試行できる。
- `pnpm check` がテスト、問題検査、静的トップを含む本番ビルドまで完走する。
- Cloudflareへは承認後に検証済みの `dist/` だけを公開する。

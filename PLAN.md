# 暗算ポーカー 仕様

**暗算ポーカー｜勝率と確率のドリル**

2択で答える確率ドリル。既存アプリの不満（4択・文字だけ・専門的すぎる）を解消する。

---

## 1. 仕様

| 項目 | 内容 |
|---|---|
| 出題 | プリフロップ・フロップ・ターンを**ランダムに混ぜる** |
| 構成 | 1セットに**モードA・B・C を混ぜる**（比率は固定する） |
| 問い | リバーまでに役が完成する確率に近いのは？ |
| 回答 | **2択**。分数から選ぶ |
| 1セット | **10問** |
| 時間 | **制限なし。回答時間を計測するだけ** |
| ボード | **段階的に表示**（フロップ3枚 → 一拍 → ターン1枚） |
| 問題 | 事前に選別したものだけ。ランダム生成しない |

別モードとして「2つの手札を見せてどちらが強いか」がある（§5）。

### 時間を切らない理由

タイムアウトのストレスがなく、上達が「正解率」と「速度」の2軸で見える。正解率が頭打ちになった後も秒数が縮むのが分かる。

**結果は種類別に出す。** モードAとBでは自然な所要時間が違うため、1つの平均値にまとめると混ざり方で数字が動いてしまう。

```
8/10 ・ 確率 3.4秒 ・ 比較 7.1秒
```

**A7問 + B3問の比率を固定する理由もこれ。** 比率が毎回変わると平均秒数がセッション間で比較できなくなる。

厳しくやりたい人向けに、制限時間モードを別途用意してもよい。

| | 表示枚数 | 制限 |
|---|---|---|
| A プリフロップ | 2 | 3秒 |
| A フロップ | 5 | 5秒 |
| A ターン | 6 | 6秒 |
| B 全ステージ | 4〜8 | 10秒 |

### 段階表示が必要な理由

実戦ではボードが1枚ずつ増えるので、ターンで新しく処理するのは1枚だけ。6枚を初見で読ませるのは実戦より厳しい。段階表示にすると実戦と同じ認知の順番になる。計測は最後の1枚が落ちてから開始。

---

## 2. 選択肢の作り方

**ルールは1つだけ。誤答の選択肢を、正解と十分離れた値にする。** 目安は1.3倍以上。近すぎると勘で答える問題になる。

**表記は問題ごとに読みやすいほうを選ぶ。** 分数でもパーセントでも構わない。

| 正解 | 選択肢の例 |
|---|---|
| 35.0% | 1/3 か 1/4 |
| 85.2% | 約85% か 約60% |
| 8.7% | 1/12 か 1/8 |
| 49.3% | 約50% か 約30% |

小さい確率は分数のほうが直感的で、50%を超える値はパーセントのほうが読みやすい。迷ったら分数。

## 3. 実測データ

エンジンを書いて全列挙で測った値。**これが問題の正解になる。**

### フロップ（リバーまで）

| ドロー | 実測 | 正解 |
|---|---|---|
| ガットショット | 17.9% | 1/6 |
| OESD | 31.5% | 1/3 |
| フラッシュドロー | 35.0% | 1/3 |
| コンボ（FD+ガット） | 45.8% | 1/2 |
| コンボ（FD+OESD） | 54.1% | 1/2 |

### ターン（リバーまで）

| ドロー | 実測 | 正解 |
|---|---|---|
| ガットショット | 8.7% | 1/12 |
| OESD | 17.4% | 1/6 |
| フラッシュドロー | 19.6% | 1/6 |
| コンボ | 28.3% | 1/4 |
| コンボ大 | 37.0% | 1/3 |

### プリフロップ（リバーまで）

| ハンド | 実測 | 正解 |
|---|---|---|
| スーテッド → フラッシュ | 6.4% | 1/20 |
| ポケットペア → セット以上 | 11.8% | 1/8 |

### 知っておくべき性質

- **フロップの答えは5種類しかない。** 連続分布ではなく離散
- **OESD 31.5% と フラッシュドロー 35.0% は区別できない**（1.11倍）。実戦でも「どちらも3分の1」でよい
- **ターンはフロップのほぼ半分**。ただしアウツが多いほどズレる（ガットショット2.06倍 → コンボ1.62倍）。コンボだけ 1/2 → 1/4 になり「半分」が崩れる
- **ガットショットの正確な値は 16.5% ではなく 17.9%。** 教科書のアウツ計算はランナーランナーを捨てている
- **ランダムに配ると、ドローがある盤面は16.7%しか出ない。** 問題は必ず事前選別する

---

## 4. 計算エンジン

### モンテカルロを使わない

試行回数由来のブレが正解ラベルを間違える。全列挙で厳密に計算する。

| ステージ | 列挙数 |
|---|---|
| フロップ→リバー | C(47,2) = 1,081 |
| ターン→リバー | 46 |
| プリフロップ→リバー | C(50,5) = 2,118,760 |

フロップ・ターンは実行時計算で体感ゼロ。プリフロップは種類が少ないので静的テーブルでよい。

### 役判定

7枚の完全な役評価器は不要。

- **フラッシュ** … スートごとの枚数を数え、5枚以上か
- **ストレート** … ランクを13bitマスクにして5連続があるか。**Aは13と0の両方でビットを立てる**（A2345のため）

判定は**「手札を最低1枚使う」を必須**とする。ボードだけで揃ったものを完成と数えない。

### 検算

エンジンを書いたら、まずこれを再現できるか確認する。

| 状況 | 期待値 |
|---|---|
| スーテッド2枚 → フラッシュ | 6.4% |
| フロップ フラッシュドロー | 35.0% |
| フロップ OESD | 31.5% |
| フロップ ガットショット | 17.9% |
| ターン フラッシュドロー | 19.6% |
| ターン ガットショット | 8.7% |

**合わない場合の原因はほぼ2つ**: 「手札を1枚使う」条件の実装漏れ／Aをローとして数える処理の欠落。

---

## 5. モードB: 2ハンド比較

2つの手札とボードを見せて、**どちらが強いか**を選ばせる。2択が自然に成立するので分数パレットは使わない。ステージはモードAと同じくプリフロップ・フロップ・ターン。

モードAと同じ1セットに混ぜる（10問中3問）。回答は2枚のハンドそのものをタップさせればよく、モードAの分数ボタンと操作が競合しない。

### モードAとの決定的な違い

モードAは「フラッシュかストレートができたか」だけ見ればよかったが、モードBは勝敗判定なので**7枚の完全な役評価器が要る**。ワンペア同士の比較、キッカー勝負まで全部判定する必要がある。別実装。

### 計算量

| 局面 | ボード全列挙 |
|---|---|
| プリフロップ | C(48,5) = 1,712,304 |
| フロップ | C(45,2) = 990 |
| ターン | 44 |

プリフロップは1対戦で171万通り、実測で数十秒かかる。**問題バンクは必ずオフラインで焼く。** ブラウザ側は答えを読むだけ。

生成は Python + eval7（`pip install eval7`）が最短。ブラウザに評価器を載せる必要がなくなる。実行時計算したい場合のみ PHEvaluator のJSポート（PHE / 41Poker / poker-handle）を検討する。

### 出題ルール

**勝率51%未満の対戦は出さない。** 50.1%のような完全な五分は運になる。

51〜55%の際どい対戦は**1セットに2問まで**。これだけで埋まるとコイン投げになりスコアが意味を失う。ただし「ほとんど変わらない」こと自体が学びなので、排除はしない。**問題を解く時点では分からなくてよく、答え合わせの画面で驚かせる**タイプの問題として扱う。

### 実測値

| 対戦 | 勝率 | 出題 |
|---|---|---|
| AA vs KK | 82.6% : 17.4% | ○ |
| AK vs AQ | 74.0% : 26.0% | ○ |
| JJ vs AK | 57.3% : 42.7% | ○ |
| AK vs QQ | 43.2% : 56.8% | ○ |
| AKs vs TT | 46.1% : 53.9% | ○ 際どい枠 |
| AKs vs 88 | 47.7% : 52.3% | ○ 際どい枠 |
| AKs vs 44 | 48.8% : 51.2% | ○ 際どい枠 |
| AKs vs 22 | 50.1% : 49.9% | × 五分すぎ |

**AKはどのポケットペア相手でも46〜50%に収まる。** 22からQQまで13段階上がっても勝率は4ポイントしか動かない。小さいペアが不利なのはボードで2ペアができたときにカウンターフィットされ、AのキッカーでAKに負けるため。

この「ほとんど変わらない」という性質自体はクイズにできないので、**解説として見せる**。

### 教材として面白い型

- オーバーペア vs 2枚オーバーカード（AK vs QQ）
- ドミネート（AK vs AQ）
- セット vs フラッシュドロー
- トップペア vs コンボドロー

---

## 6. モードC: プリフロップ勝率

ハンドを1つ見せて、**プリフロップの勝率**を当てさせる。2人モードと6人モードの両方を出す。

### 実測値（モンテカルロ 12万試行）

| ハンド | 2人 | 6人 |
|---|---|---|
| AA | 85.2% | 49.3% |
| KK | 82.3% | 42.9% |
| QQ | 80.0% | 37.7% |
| AKs | 66.8% | 31.0% |
| AKo | 65.4% | 27.6% |
| A5s | 60.0% | 22.4% |
| 55 | 60.4% | 18.5% |
| JTs | 57.6% | 24.9% |
| 72o | 34.6% | 8.5% |

6人モードの基準は 1/6 = 16.7%、2人モードは 50%。

### 教材としての中身

**AAは6人でも約半分勝つ。** 5人相手にして五分というのは直感に反するので、問題として強い。

そして基準比を見ると、55だけ挙動が違う。

| ハンド | 2人の基準比 | 6人の基準比 |
|---|---|---|
| AA | 1.70倍 | 2.96倍 |
| AKs | 1.34倍 | 1.86倍 |
| 55 | 1.21倍 | **1.11倍** |

ほぼ全てのハンドは人数が増えると基準比が上がるが、**小さいペアだけ下がる**。55は「誰もペアを作らなければ勝てる」ハンドなので、2人なら相手1人がミスすれば済むが、6人だと5人全員がミスする必要がある。実質セットにならないと勝てなくなる。

**小さいペアは人数が増えると弱くなり、大きいペアは相対的に強くなる。** これがモードCで教えられる一番の中身。

### 計算方法

相手が複数いる場合の全列挙は非現実的なので、**モンテカルロでよい**。問題バンクはオフラインで焼くので試行回数を十分に取れる（12万試行で誤差0.1%程度）。モードAと違って閾値判定がないため、わずかな誤差が正解ラベルを変えることもない。

---

## 7. 問題データ

```js
{
  stage: 'flop',
  hole: ['Ah','Kh'],
  board: ['2h','9h','Jd'],
  target: 'flush',
  trueP: 34.97,
  answer: '1/3',
  distractor: '1/4',
  category: 'flush_draw',
  explain: 'ハートが4枚。残り9枚のどれかが2回のうちに来ればいい。'
}
```

問題文には**狙う役を必ず書く**。「リバーまでに**フラッシュ**が完成する確率に近いのは？」

コンボドローは「**フラッシュかストレート**」と書く。コンボだと分かってしまうが、そこから 1/3 か 1/2 かを選ぶ仕事は残るので実質ヒントにならない。回答時間が短いので、何を狙うかを迷わせないほうが優先。

---

## 8. 実装フェーズ

| # | やること | 完了条件 |
|---|---|---|
| 1 | **確率エンジンを純粋関数で書く**（UIゼロ） | §4 の6つの検算値が一致 |
| 2 | 問題バンクの選別と生成 | 面白い問題が100問程度 |
| 3 | **簡略UI（文字だけ）で遊べるものを作る** | 10問通しで遊べて、面白いか判断できる |
| 4 | 本番UI（カード表示・段階表示） | 10問完走できる |
| 5 | 時間計測とスコア | 正解率と平均秒数が出る |
| 6 | カテゴリ別正答率 | どのドローを過大評価しているか見える |
| 7 | **モードB・C**（問題バンク生成 → UI追加） | — |

### フェーズ3について

本番UIを作る前に、`♥A ♥K` のような**文字だけの表示でゲーム性を確認する**。ここで面白くなければ、UIを作り込んでも面白くならない。

エンジンと問題バンクを先に済ませておくことで、**本物の数字で判断できる**。手書きの数字で遊んで面白いと思っても、その数字が間違っていたら判断ごと無意味になる。エンジンはどのみち必要なので先に書いても無駄にならない。

カードは `♥A` `♥K` `♥T` のように**本番と同じ表記**にする。`♥1` で慣れると本番UIに移ったとき読み直しになる。

この段階で分かること: 問題が面白いか / 10問という長さ / モードA・B・Cが混ざって成立するか / 解説が読みたくなるか

**分からないこと: 制限時間が適切かどうか。** 文字とカード画像では読む速度が違うため、タイミングは本番UIで測り直す。簡略版では時間を切らず計測のみ。

フェーズ1が土台。ここが正しければ後は載せるだけ。

---

## 9. 技術構成

- 素のHTML + JS 単一ファイルで十分
- GitHub Pages に置けば無料で公開でき、スマホのホーム画面に追加できる
- 問題バンクの生成のみ Python
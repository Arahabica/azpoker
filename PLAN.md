# 暗算ポーカー 仕様

**暗算ポーカー｜勝率と確率のドリル**

ポーカーで実際に考える確率を、カードを見ながら2択で答える。画面と文言は [`DESIGN.md`](DESIGN.md)、起動とコード構成は [`README.md`](README.md) を正とする。

## セッション

| 項目 | 内容 |
|---|---|
| 1セット | 10問、各問題に制限時間あり |
| モード | A 5問、B 2問、C 1問、D 2問 |
| ステージ | プリフロップ3問、フロップ4問、ターン3問 |
| 難易度 | 中8問、難2問 |
| 0% | 一見可能そうな0%問題は最大1問 |
| 重複 | Aは別カテゴリ、Dは別系統から選び、直近500問を再出題しない |

### 制限時間

各問題には制限時間を設け、回答可能になってから回答または時間切れまでを計測する。各問の時間は保存せず、1セット内の合計だけを結果画面へ表示する。平均時間は表示しない。1セットの合計制限時間は揃えず、出題内容に応じて変わってよい。

| 問題 | 制限時間 |
|---|---:|
| 1ハンド・中・プリフロップ | 5秒 |
| 1ハンド・中・フロップ | 8秒 |
| 1ハンド・中・ターン | 12秒 |
| 難問 | 16秒 |
| 2ハンドを表示するモードB | 16秒 |

- カード、問題文、選択肢の表示が完了し、回答可能になった時点から開始する。表示アニメーション中の時間は含めない。
- ヘッダーは10問のセグメントと残り時間を統合する。全セグメントの太さを揃え、現在の問題は5・8・12・16秒に比例した横幅にする。秒数は表示せず、内部の白いゲージを左から右へ伸ばす。
- 回答済みは正解を緑、不正解と時間切れを赤で残す。残り3秒から画面全体へ赤い点滅を重ね、残り1.5秒では点滅を速く強くする。
- 0秒になった問題は時間切れの不正解とし、通常の回答と同じく実際の値と解説を表示する。
- 回答、時間切れ、次の問題への移動、トップへの離脱時にはタイマーを確実に停止し、二重回答を起こさない。
- 問題画面に常設の戻る操作は置かない。回答パネル右上の退出アイコンから確認パネルを開き、「トップページに戻る」「問題を続ける」を選べるようにする。

### 結果

- 正答数と全10問、回答に使った合計時間と合計制限時間をそれぞれ大小2段で表示する。
- 時間切れが1問以上ある場合だけ、合計制限時間の下に回数を表示する。
- 速度を褒める見出しは9問以上正解かつ時間切れ0問の場合だけ使う。8問以下では回答時間にかかわらず正答数に合う見出しを使う。
- 全問正解は専用の王冠、黄色の強調、紙吹雪でほかの結果より派手に祝う。動きを減らす設定では紙吹雪を表示しない。

## 問題バンク

問題は生成時に確率と勝敗を計算し、本番ではA・B+C・Dから100問ずつ取得する。初期JavaScriptへ問題本体を含めず、取得した300問は複数セッションで再利用する。

| モード | 問題数 | 内容 |
|---|---:|---|
| A | 10,000 | 役、ランク、ボード変化の確率 |
| B | 4,800 | 2つの手札の比較と、逆転・引き分けなどの確率 |
| C | 1,200 | プリフロップとポストフロップの2人・6人勝率 |
| D | 4,000 | 相手のランク、完成役、ドローの可能性 |
| 合計 | 20,000 | 100問単位のJSON 200ファイル |

### モードA

| カテゴリ | 問題数 |
|---|---:|
| フラッシュ | 1,400 |
| ストレート | 1,600 |
| フラッシュまたはストレート | 800 |
| 指定ランクが出る | 550 |
| 指定ランクがスリーになる | 500 |
| ツーペア | 400 |
| フルハウス | 400 |
| フォーカード | 250 |
| ストレートフラッシュ | 100 |

通常のドロー、バックドア、ランナーランナー、重複アウツ、残り枚数不足による0%を含める。すでに対象が完成している状況は出さない。

加えて、残り2枚を両方使うストレート700問・フラッシュ650問、ボードのペア・ツーペア、オーバーカード、4フラッシュ、カウンターフィット、役の維持を扱う。ナッツフラッシュ問題は作らない。

### モードB

「勝率が高いのは？」3,000問に加え、数値回答を7形式・合計1,800問用意する。引き分け、左右いずれかの手札の勝率、最後の1枚での逆転、次のカードで役の強さが逆転する確率、ボードのストレート・フラッシュによる引き分けを扱う。「両方の役の種類が同じになる確率」は出題しない。引き分け、現在負けている手札、現在勝っている手札は各300問、ほか4形式は各225問とする。1セットには手札選択型と数値型を1問ずつ出す。51〜55%の接戦も、複数要素が競合する難問として残す。

### モードC

169種類の初期ハンドを2人・6人で1問ずつ用意し、さらにフロップ・ターンの2人卓431問、6人卓431問を用意する。スート違いで水増しせず、別人数の勝率を誤答へ流用しない。例えばA♥5♥・2人は約60%対約45%とする。

### モードD

指定ランクに加え、ポケットペア、オーバーペア、セット、ボードで一番高いランクを持つ可能性、ツーペア、ストレート、フラッシュ、OESD、ガットショット、フラッシュドロー、コンボドロー、上のフラッシュ、同じペアで上のキッカー、全員のミス、指定ランクを持つ人数を扱う。相手の役・ドローはホールカードを最低1枚使う。ハンドレンジやアクションは考慮しない。

相手の人数は問題文だけで判断できるよう、すべて明記する。読点は入れず、「2人卓で相手が5を持つ確率は？」「6人卓でほかの誰かが5を持つ確率は？」を基準とする。

## 選択肢

概算値は `0 / 1 / 2 / 3 / 5 / 7.5 / 10 / 12.5 / 15 / 17.5 / 20 / 25 … 100%` から、実際の値に最も近いものを使う。回答後は小数1桁を表示する。

誤答には必ず `distractorModel` を持たせ、次のような典型的な勘違いから作る。

- 残り1枚と2枚を混同する
- 重複アウツを二重に数える
- バックドアを通常ドローとして数える
- 見えている同ランクを引き忘れる
- 6人卓で相手1人分しか考えない
- キッカー、完成役、強いドローの一方を過大評価する

数値差だけで難しくしたり、別人数・別ステージの値を捨て選択肢として流用したりしない。
正解が20%以上なら選択肢同士を最低10ポイント、5〜17.5%なら最低5ポイント、1〜3%なら最低2ポイント離す。実際が1.9%なら正解は約2%とし、誤答には隣接する1%・3%ではなく5%以上を使う。0%の誤答は3%以上とする。

## 計算と生成

- AとBのフロップ・ターンは残りカードを全列挙する。
- Bのプリフロップは12,000試行、Cは30,000試行の固定seedモンテカルロ。Cはハンド単位で並列計算する。
- Dの2人卓は相手の2枚を全列挙し、6人卓は重複なしで5人へ配る固定seedモンテカルロを使う。
- `pnpm questions:build` で生成し、`pnpm check:bank` は生成済み20,000問と3パックmanifestを高速検査する。
- 同型判定ではスートを初出順に正規化し、スート名だけを置換した問題を重複させない。

## 初心者向け文言

「3カード」は使わず「スリー」とする。専門用語を使う場合は、日本語の意味を先に書く。

「スート」は表示せず、一般的な説明では「同じマーク」、対象が決まっている問題では「ダイヤ」「ハート」のように具体的なマーク名を書く。

「ランク」は表示せず、「同じ数字や文字」「ボードで一番強いカード」のようにカードを見れば分かる言葉へ置き換える。

問題文は見ただけで対象を判断できる短さにし、用語や条件の読解そのものを問題にしない。表示上は意味のまとまりを改行不可にし、まとまりの間へだけ `<wbr>` を置く。

- 両端のどちらでも完成する形（OESD）
- 内側の1ランクだけを待つ形（ガットショット）
- 内側の待ちが2種類ある形（ダブルガット）
- 残り2枚が両方そろって完成する形（ランナーランナー）
- 複数の役を同時に待つ形（コンボドロー）

## 完了条件

- 20,000問とモード・カテゴリ別件数が一致する。
- A 100ファイル、B+C 60ファイル、D 40ファイル、manifest、ID、同型除外キーが整合する。
- 全問に有効なカード、正解、誤答理由、難易度がある。
- 10問が固定モード・ステージ・考え方比率と、0%最大1問を満たす。
- 各問題が5秒・8秒・12秒・16秒の制限に従い、時間切れを不正解として処理できる。
- 回答時間は永続化せず、結果画面に1セットの合計だけを0.1秒単位で表示する。平均時間は表示せず、1セットの合計制限時間も調整しない。
- JSON取得失敗から再試行できる。
- `pnpm check` がテスト、問題検査、静的トップを含む本番ビルドまで完走する。
- Cloudflareへは承認後に検証済みの `dist/` だけを公開する。
